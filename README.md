Electron.js app with React, Redux and Express with postgreSQL.

To start:

```
    yarn
```

add your postgresql url to database to "src/srver/db/secret.ts"

```
    yarn make
```

open "out/\*\*/el-todo.exe"
